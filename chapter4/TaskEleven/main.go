package main

import "fmt"

func main() {

	fmt.Println("When you divide an integer with another in Go \n the fractional part is truncated \n to avoid that convert one of the operands to float before dividing")
}
