package main

import "fmt"

func main() {

	fmt.Println(`
	(a) Sequence, selection and iteration,
	(b) 'if' and 'else' statement,
	(c) definite control,
	(d) sentinel, 
	(e) sequence structure,
	(f) zero,
	(g) prefix,
	(h) 8`)
}
