package main

import "fmt"

func main() {

	fmt.Println(`
	(a) brackets are not allowed in if condition, no need for semicolon also missing curly braces in the if and else block, also a quote after the bracket in else statement
	(b) no while loop in GO
	(c) no while loop on Go
	(d) no while iteration statement in GO`)
}
